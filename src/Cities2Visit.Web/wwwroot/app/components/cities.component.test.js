describe('Cities component', function () {

    beforeEach(module('app'));

    var Cities;
    
    beforeEach(inject(function ($componentController){
    
    }));

    it('should exist', function () {
        expect(Cities).toBeDefined();
    });
});